let aiHistory=[];function toggleAIChat(){document.getElementById('ai-chat').classList.toggle('show')}function openAIChat(message){document.getElementById('ai-chat').classList.add('show');document.getElementById('ai-input').value=message;sendAIMessage()}function addMessage(text,type){const box=document.getElementById('ai-messages');const div=document.createElement('div');div.className=type;div.textContent=text;box.appendChild(div);box.scrollTop=box.scrollHeight}async function sendAIMessage(){const input=document.getElementById('ai-input');const message=input.value.trim();if(!message)return;input.value='';addMessage(message,'user');addMessage('Pensando...','bot');try{const response=await fetch('/.netlify/functions/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message,history:aiHistory})});const data=await response.json();const bots=document.querySelectorAll('.bot');bots[bots.length-1].textContent=data.reply||'No pude responder en este momento.';aiHistory.push({role:'user',content:message});aiHistory.push({role:'assistant',content:data.reply||''});aiHistory=aiHistory.slice(-10)}catch(e){const bots=document.querySelectorAll('.bot');bots[bots.length-1].textContent='Ahora mismo no puedo conectar con la IA. Revisa que OPENAI_API_KEY esté guardada en Netlify y que la función esté publicada.'}}function sendWhatsApp(){const nombre=document.getElementById('nombre')?.value||'';const pais=document.getElementById('pais')?.value||'';const tramite=document.getElementById('tramite')?.value||'';const urgencia=document.getElementById('urgencia')?.value||'';const mensaje=document.getElementById('mensaje')?.value||'';const text=`Hola, vengo desde la web de Trámites Alejandro.%0A%0AQuiero solicitar presupuesto.%0A%0ANombre: ${encodeURIComponent(nombre)}%0APaís donde estoy: ${encodeURIComponent(pais)}%0ATrámite: ${encodeURIComponent(tramite)}%0AUrgencia: ${encodeURIComponent(urgencia)}%0AMensaje: ${encodeURIComponent(mensaje)}`;window.open(`https://wa.me/5355335822?text=${text}`,'_blank')}